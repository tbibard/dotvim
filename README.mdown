DOTVIM Personal vim config
==========================

Install
-------

```
git clone https://github.com/tbibard/dotvim.git $HOME/.vim
git clone https://github.com/gmarik/vundle.git $HOME/.vim/bundle/vundle
ln -s $HOME/.vim/vimrc $HOME/.vimrc
:PluginInstall
```

Update
------

```
cd $HOME/.vim
git pull
```
